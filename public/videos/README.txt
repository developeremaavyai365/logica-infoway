Place the Logica brand film here:

  logica-showcase.mp4   (required — the full-screen video)
  logica-showcase.jpg   (optional — poster shown before the video loads)

Recommended: 1920x1080 (or 4K) H.264 MP4, muted-friendly, ~15-40s loop.
The homepage <VideoShowcase /> section auto-plays it (muted, looping) with a
mute/pause control. Until the file is added, an animated brand gradient +
overlay acts as a graceful fallback.
